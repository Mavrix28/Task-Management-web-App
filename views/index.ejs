<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900  text-white px-5">
        <div class="form px-10  py-10">
            <h1 class="text-5xl p-5 text-zinc-400 select-none">Task Management web App</h1>
            <form action="/create" method="post">
                <input type="text" class="block mx-5  w-full  h-10 rounded-md p-3 outline-none bg-zinc-800"
                    placeholder="Title" name="Title">
                <textarea class="block mx-5 my-5 resize-none w-full h-20 rounded-md p-3 outline-none bg-zinc-800"
                    placeholder="Type your task detail" name="details"></textarea>
                <input type="submit" class="bg-blue-400  ml-5 px-3 py-2 rounded-md">
            </form>
        </div>
        <div class="tasks px-14  py-5  flex gap-10 flex-wrap">

            <% if (files.length>0) { %>
                <% files.forEach(function(val){ %>
                    <div class="task w-70   rounded-md bg-zinc-600 px-3">
                        <h1 class="text-3xl text-white ">
                            <%= val %>
                        </h1>
                        <div class="option flex space-between mt-3 mb-2 gap-5 ">
                            <a class="text-blue-500   text-sm " href="/files/<%= val %>">Read more</a>
                            <a class="text-yellow-400   text-sm " href="/edit/<%= val %>">Edit Title</a>
                            <a class="text-red-400   text-sm " href="/delete/<%= val %>">Delete</a>
                        </div>
                    </div>
                    <% }); %>
                        <% }else{ %>
                            <h1 class="text-zinc-600 text-3xl">No task yet</h1>
                            <% } %>

        </div>
        

</body>

</html>